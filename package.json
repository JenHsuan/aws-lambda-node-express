{
  "name": "aws-lambda-node-express",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "serverless offline",
    "build": "rimraf dist && tsc",
    "deploy": "export AWS_ACCESS_KEY_ID=xxx && export AWS_SECRET_ACCESS_KEY=xxx && serverless deploy --region ap-southeast-2"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.645.0",
    "express": "^4.19.2",
    "lodash": "^4.17.21",
    "serverless-http": "^3.2.0",
    "serverless-offline": "^13.8.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
